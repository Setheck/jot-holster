{
  "name": "jot-holster",
  "version": "0.5.1",
  "description": "JOT Holster browser extension — unit tests live in ./tests.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:ui": "playwright test",
    "test:ui:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
